export type TutorialModeKey = 'parallel' | 'reverse' | 'diagonal'

export type CarPose = {
  x: number
  y: number
  angle: number
}

export type SimulationOptions = {
  width: number
  height: number
}
